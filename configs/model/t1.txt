# settings for TreeMort-3T-UNet (self attention unet with flair backbone)

include = base_config.txt

model = flair_unet_sdt

run-id = switzerland_rgb_t1

resume = True

input-channels = 3
output-channels = 3

freeze-epochs = 3
keep-first-encoder-blocks = 1

resume-from = ${TREEMORT_OUTPUT_DIR}/flair_unet_sdt/finland_rgb_b0/best.weights.pth
