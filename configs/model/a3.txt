# Transfer run on Swiss data downsampled to 25cm equivalent

include = base_config.txt

model = flair_unet_sdt

run-id = switzerland_25cm_a3

resume = True

input-channels = 3
output-channels = 3

freeze-epochs = 3
keep-first-encoder-blocks = 1

resume-from = ${TREEMORT_OUTPUT_DIR}/flair_unet_sdt/finland_rgb_b0/best.weights.pth

downsample-factor = 0.4
