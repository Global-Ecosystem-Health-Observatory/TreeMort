# Transfer run on Swiss data (immediate unfreeze vs progressive)

include = base_config.txt

model = flair_unet_sdt

run-id = switzerland_rgb_a4

resume = True

input-channels = 3
output-channels = 3

freeze-epochs = 0
keep-first-encoder-blocks = 0

resume-from = ${TREEMORT_OUTPUT_DIR}/flair_unet_sdt/finland_rgb_b0/best.weights.pth
