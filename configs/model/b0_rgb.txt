# settings for TreeMort-3T-UNet (self attention unet with flair backbone)

include = base_config.txt

model = flair_unet_sdt

run-id = finland_rgb_b0

resume = False

input-channels = 3
output-channels = 3

# Use 4-channel Finland HDF5 but train on RGB only
channel-indices = [1, 2, 3]

freeze-epochs = 0
keep-first-encoder-blocks = 0
