# configurations for inference creation for US data

window-size = 256
stride = 128
nir-rgb-order = [3, 0, 1, 2]

best-model = ./output/flair_unet/best.weights.usa.distill.pth

model-config = ./configs/flair_unet_bs8_cs256.txt

min-area-threshold=2.0  # For chaparral or mixed ecosystems
max-aspect-ratio=4.0    # Compact shapes typical of Californian trees
min-solidity=0.80       # Focus on dense, contiguous canopies
min-distance=4
