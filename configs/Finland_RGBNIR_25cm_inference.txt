# configurations for inference creation

window-size = 256
stride = 256
nir-rgb-order = [3, 0, 1, 2]

best-model = ./output/flair_unet/best.weights.finland.pth

model-config = ./configs/flair_unet_bs8_cs256.txt

min-area-threshold=1.0  # Smaller canopies typical of boreal species
max-aspect-ratio=3.0    # Compact shapes expected for Finnish tree canopies
min-solidity=0.85       # Focus on dense, contiguous canopies
min-distance=7
